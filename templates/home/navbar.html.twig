    <!--====== Bootstrap CSS ======-->
    <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
    
    <!--====== Style CSS ======-->
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">

    <link rel="icon" href="{{asset('images/show.png')}}" />
    
    <!--====== NAVBAR TWO PART START ======-->

        
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <nav class="navbar navbar-expand-lg">
                       
                        <a class="navbar-brand" href="{{path('home')}}">
                            <img src="{{asset('images/logo.png')}}" width=64 alt="Logo">
                        </a>
                        
                        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTwo" aria-controls="navbarTwo" aria-expanded="false" aria-label="Toggle navigation">
                            <span class="toggler-icon"></span>
                            <span class="toggler-icon"></span>
                            <span class="toggler-icon"></span>
                        </button>

                        <div class="collapse navbar-collapse sub-menu-bar" id="navbarTwo">
                            <ul class="navbar-nav m-auto">
                                <li class="nav-item"><a class="page-scroll" href="{{path('home')}}">Home</a></li>
                                <li class="nav-item"><a class="page-scroll" href="{{path('series_index')}}">Series</a></li>
                                {% if app.user %}
                                <li class="nav-item"><a class="page-scroll" href="{{path('series_my')}}">My Series</a></li>
                                {% else %}
                                <li class="nav-item"><a class="page-scroll" href="{{path('app_register')}}">Register</a></li>
                                {% endif %}
                                {% if is_granted('ROLE_ADMIN') %}
                                <li class="nav-item"><a class="page-scroll" href="{{path('admin_panel')}}">Admin panel</a></li>
                                {% endif %}
                                <li class="nav-item"><a class="page-scroll" href="{{path('app_about')}}">About</a></li>
                                
                            </ul>
                        </div>
                        
                        <div class="navbar-btn d-none d-sm-inline-block">
                            <ul>
                                {% if app.user %}
                                    <li class="nav-item"><a class="page-scroll" href="{{path('app_logout')}}">Logout</a></li>
                                {% else %}
                                    <li class="nav-item"><a class="page-scroll" href="{{path('app_login')}}">Login</a></li>
                                {% endif %}
                            </ul>
                        </div>
                    </nav> <!-- navbar -->
                </div>
            </div> <!-- row -->
        </div> <!-- container -->
        
        
        
    <!--====== Jquery js ======-->
    <script src="{{asset('js/vendor/jquery-1.12.4.min.js')}}"></script>
    <script src="{{asset('js/bootstrap.min.js')}}"></script>
    <script src="{{asset('js/main.js')}}"></script>