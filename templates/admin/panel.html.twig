{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}

    <section class="navbar-area p-3 mb-2 bg-primary text-white">
        {% include 'home/navbar.html.twig' %}
    </section>
    <br>
    <br>
    <br>
    <div class="jumbotron text-center mt-2">
        <div class="container">
            <div class="row">
                <div class="col-12">
                    <h1>Welcome {{ admin.name }}</h1>
                    <br>
                    <h3>What do you want to do ?</h3>
                </div>
            </div>
        </div>
    </div>

    <div class="d-flex justify-content-center">
        <div class="single-features mt-40">
            <div class="features-title-icon text-center ">
                <h3>Moderate Comments</h3>
                <br>
                <div class="d-flex justify-content-center">
                    <a href="{{ path('admin_comments') }}" class="btn btn-outline-primary">Let's Go</a>                                     
                </div>
            </div>
        </div>

        <div class="single-features mt-40">
            <div class="features-title-icon text-center ">
                <h3>Add Series</h3>
                <br>
                <div class="d-flex justify-content-center">
                    <a href="{{ path('admin_series_add') }}" class="btn btn-outline-primary">Let's Go</a>                                     
                </div>
            </div>
        </div>
    </div>
    
{% endblock %}
